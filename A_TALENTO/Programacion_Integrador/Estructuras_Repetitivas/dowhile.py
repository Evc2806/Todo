# Inicializamos la variable de control

i = 8

# Repetimos el bloque al menos una vez
while True:
    # Imprimimos el valor de i
    print(i)
    # Incrementamos el valor de i para avanzar en el ciclo
    i += 1
    # Si i supera 5, salimos del ciclo
    if i > 5:
        break